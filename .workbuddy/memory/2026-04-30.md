# 2026-04-30 工作记录

## hotfix：JS 语法错误导致页面白屏（加载遮罩卡死在0%）

- **问题**：commit `df45f94` 在修改年维度探测按钮时，replace_in_file 漏掉了 `html += '</div>'; contentEl.innerHTML = html; });` 三行
- **现象**：整个 bindEvents 函数语法错误 → JS 全部无法执行 → 加载遮罩永远不隐藏 → 页面卡在"连接收银系统 API 0%"
- **修复**：commit `c6b727d` 补全缺失的三行
- **教训**：修改按钮事件处理器（尤其是 replace_in_file 截断操作）后，必须检查 click handler 是否正确闭合（`});`）

## API文档已确认

- ShowDoc API 可通过 `https://doc.wuuxiang.com/showdoc/server/index.php?s=/api/page/info&page_id=14375` 获取页面内容（无需登录）
- dateType=1：按自然日查询（单日数据）
- dateType=2：按营业日查询（当年整月汇总有效，历史年返回0）
- settleDate 与 beginDate 都填时，以 settleDate 为准
- dateType=3 不存在于官方文档（之前用的是错误值）

## 历史年 API 探测验证（2026-04-30）

- **探测结论**：getBusinessSituation 对 2025 历史年份全部返回 0（dateType=1、dateType=2 均为 0，HTTP 200）
- **最终方案**：历史年改回 getserialdata 逐天查询，纯收 = last_total - not_income_money（带分页翻页）
- **缓存版本**：v9 → v10，强制清除旧的错误数据
- **教训**：getBusinessSituation 只支持当年数据；历史年唯一可用的 API 就是 getserialdata
