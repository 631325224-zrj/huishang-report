# 2026-04-10 工作日志

## 数据验证完成（全部通过）

- 用户导出了 CSV 文件「徽商故里_全部门店_2026-04-10.csv」进行数据比对
- 28 门店 3月/2月 数据，与收银系统后台和历史文档验证值**全部一致**
- 广安门店 2月 1,374,131.89 已确认与收银系统后台吻合（并非重复累加 bug）
- 全部区域小计 → 总计加减校验无误差

## 报表相关修复（本日）

### 年报数据偏小 bug 修复
- 问题：年报维度只调用 `ensureMonthData(year, 1)`，只触发1月加载，2025年6~9月数据从未加载
- 修复：年报维度切换时循环加载选中年份的所有有效月份（从上线月到当前月）
- 代码位置：`renderSummary` 函数内的 `STATE.dim === 'year'` 分支



- 修复第 1400 行语法错误（模板字符串闭合缺失导致 `type: 'line'` 报错）
- 回滚趋势图动态标题和预加载刷新逻辑（用户要求回滚）
- 预加载范围回滚到 6 个月，缓存版本回滚到 v4
- 修复 `availableYears` 为空时 `Math.min(...[])` 返回 Infinity 的边界保护
